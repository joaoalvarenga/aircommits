{
  "publisher": "joaoalvarenga",
  "name": "aircommits",
  "displayName": "AirCommits",
  "description": "Connect with other developers at the same airport or on the same flight.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onUri",
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "aircommits.auth",
        "title": "Hello World"
      },
      {
        "command": "aircommits.login",
        "title": "Login with GitHub"
      },
      {
        "command": "aircommits.login.callback",
        "title": "Callback for Login with GitHub"
      },
      {
        "command": "aircommits.openSettings",
        "title": "Open AirCommits Settings"
      },
      {
        "command": "aircommits.openFeed",
        "title": "Open AirCommits Feed"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "aircommits-sidebar",
          "title": "AirCommits",
          "icon": "images/airplane.svg"
        }
      ]
    },
    "views": {
      "aircommits-sidebar": [
        {
          "type": "webview",
          "id": "aircommits.feed",
          "name": "Feed"
        }
      ]
    },
    "configuration": {
      "title": "AirCommits",
      "properties": {
        "aircommits.serverUrl": {
          "type": "string",
          "default": "http://localhost:3001",
          "description": "AirCommits server URL"
        },
        "aircommits.autoDetectLocation": {
          "type": "boolean",
          "default": true,
          "description": "Automatically detect user location"
        },
        "aircommits.manualAirport": {
          "type": "string",
          "description": "Manually set airport code (e.g., GRU, JFK)"
        },
        "aircommits.manualFlight": {
          "type": "string",
          "description": "Manually set flight number (e.g., LATAM 8001)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "20.x",
    "@types/vscode": "^1.101.0",
    "@typescript-eslint/eslint-plugin": "^6.4.0",
    "@typescript-eslint/parser": "^6.4.0",
    "@vscode/test-electron": "^2.3.4",
    "eslint": "^8.47.0",
    "glob": "^10.3.3",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
